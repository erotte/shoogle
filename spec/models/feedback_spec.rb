require 'spec_helper'

describe Feedback do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should instanciate a new instance given valid attributes" do
    Feedback.new(@valid_attributes)
  end
end
