%h1 Passwort vergessen?
- form_for resource_name, resource, :url => password_path(resource_name) do |f|
  = f.error_messages
  %ul
    %li.labelwrap
      = f.label :email
      = f.text_field :email
    %li.buttonwrap
      = f.submit "Anweisungen zusenden", :class => 'awesome red right_submit'
= render :partial => "shared/devise_links"
