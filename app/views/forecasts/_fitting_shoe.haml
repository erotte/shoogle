- content_for :head do
  = javascript_include_tag 'jquery.plugins/jquery.jcarousel.min.js'
  = stylesheet_link_tag  'jcarousel/ssf/skin.css'
  :javascript
    $(document).ready(function() {
      $('#fitting_shoes').jcarousel({
        visible: 5
      });                                
    });

%h2 Passende Schuhe:
%table.fitting_shoes
  %thead
    %tr
      %th Hersteller
      %th Modell
      %th Treffer
      %th
  %tbody    
    -@foot.recommended_shoes.each do |fitting_shoe|

      %tr{ :class => cycle('odd','even') }
        %td=fitting_shoe.manufacturer.capitalize
        %td=fitting_shoe.model.capitalize
        %td=fitting_shoe.num_feet
        %td.shoes
          - if fitting_shoe.affiliate_shoes.present?
            %ol#fitting_shoes.results.jcarousel-skin-ssf
              =render 'affiliate_shoe/show', :affiliate_shoes => fitting_shoe.affiliate_shoes
          - else
            %p.not_found
              = image_tag 'app/not_available.png', :title => "Dieser Schuh ist leider nicht verfügbar"
              %span Leider keine passenden Schuhe gefunden
