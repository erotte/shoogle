=debug_toggle @foot

/ -semantic_remote_form_for @foot, :update => 'forecast_shoes' do |form| 
/   =form.inputs :manufacturer, :model, :size, :for => :shoe 
/   =form.buttons
 
=render :partial => 'requested_shoe_form'


#forecast_shoes_form
  = render :partial => 'add_shoe_form' unless shoes_blank?
= link_to "Löschen", :action => :new, :controller => :forecasts
%table#forecast_shoes.shoes
  =render :partial => 'feet/shoe', :collection => @foot.shoes.delete_if{|shoe| shoe.new_record?} 

#show_result_button.tar{:class => "#{ shoes_blank? ? 'hide' : ''}"  } 
  =link_to "Ergebnis Anzeigen", fitting_url(@foot)
  %br
  =link_to "Alle passenden Schuhe", more_fitting_url(@foot)
#forecast_result 